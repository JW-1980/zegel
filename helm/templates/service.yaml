apiVersion: v1
kind: Service
metadata:
  name: {{ include "zegel.fullname" . }}
  labels:
    {{- include "zegel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zegel.selectorLabels" . | nindent 4 }}
