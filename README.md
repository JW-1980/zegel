# zegel
Zegel ("seal" in Dutch) is a tamper-proof container format. It wraps any file and makes the content physically unreadable if even a single byte is modified after creation. This is not a "check and warn" system - the cryptographic math itself prevents decoding. Support for all major operating systems comes with the package
