1. Review existing `SUGGESTED_IMPROVEMENTS.md` and `website/IMPROVEMENTS.md` (which lists already implemented improvements).
2. Generate a new set of 100 software improvements, ensuring they do NOT overlap with the 100 items listed in `website/IMPROVEMENTS.md` or any existing Flutter app features.
3. Generate a new list of 15 items for easier installation/hosting.
4. Generate 20 redesign recommendations for Flutter screens with reasons.
5. Generate 20 redesign recommendations for Laravel website pages with reasons.
6. Overwrite the `SUGGESTED_IMPROVEMENTS.md` with these newly generated, unique items that strictly adhere to constraints (no AI, not already implemented).
7. Execute pre-commit checks.
8. Submit the new file to the branch.
