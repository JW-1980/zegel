name: zegel_cli
description: Command-line interface for the Zegel tamper-proof container format.
version: 1.2.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  zegel:
    path: ../lib
  args: ^2.4.2
  crypto: ^3.0.3
  pointycastle: ^3.7.3

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
